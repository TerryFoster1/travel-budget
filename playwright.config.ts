// Playwright configuration placeholder for future end-to-end coverage.
// Add real projects, base URLs, and reporters once user-facing flows exist.
import { defineConfig } from "@playwright/test";

export default defineConfig({
  testDir: "./tests/e2e"
});
